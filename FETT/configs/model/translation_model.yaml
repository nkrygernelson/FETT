_target_: src.fett.model.lightning_module.FettTranslationModule

# Path to a trained base model checkpoint (must contain embedded model_cfg).
# Override at runtime: uv run invoke train-translation model.base_model_checkpoint=models/best.ckpt
base_model_checkpoint: ???

head:
  hidden_dim: 128
  dropout: 0.1
