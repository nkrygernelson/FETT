# Path to the trained base model checkpoint (must contain embedded model_cfg).
# Override at runtime, e.g. model.base_model_checkpoint=models/best.ckpt
base_model_checkpoint: ???

head:
  hidden_dim: 128
  dropout: 0.1
